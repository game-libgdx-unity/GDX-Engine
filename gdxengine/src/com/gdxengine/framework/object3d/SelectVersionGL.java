package com.gdxengine.framework.object3d;

/**
 * Render the object base the OpenGL ES version
 * 
 * @author Vinh
 *
 */
public interface SelectVersionGL {
	/**
	 * Render the object base the OpenGL ES version
	 * @param gameTime time span between the current frame and the 
	 */
	public void render(float gameTime);
}
